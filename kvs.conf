# 基础网络
bind 0.0.0.0
port 6380

# reactor | proactor | ntyco
network reactor

# system | jemalloc | mypool
# 目前仅支持system and jemalloc, mypool is coming
allocator jemalloc

appendonly yes
appendfilename appendonly.aof
appendfsync always